import {BoardState} from './BoardState'
import {GameTime, setGameTime} from './GameTime'
import {OpponentName, setOpponentName} from './OpponentName'
import {PlayerColor, setPlayerColor} from './PlayerColor'
import {ValidCheckMoves,updateValidCheckMoves} from './ValidCheckMoves'
import {LastCaptureMove, updateLastCaptureMove} from './LastCaptureMove'

export {
  BoardState,
  GameTime,
  setGameTime,
  OpponentName,
  setOpponentName,
  PlayerColor,
  setPlayerColor,
  ValidCheckMoves,
  updateValidCheckMoves,
  LastCaptureMove, 
  updateLastCaptureMove
}